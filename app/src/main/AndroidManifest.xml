<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abhi.weakbrain">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".WeakBrain"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".FirstpageActicity" />
        <activity android:name=".SecondpageActivity" />
        <activity android:name=".ThirdpageActivity" />
        <activity android:name=".ExampaperActivity" />
        <activity android:name=".EbookActivity" />
        <activity android:name=".StudentsearchActivity" />
        <activity android:name=".ExampaperoneActivity" />
        <activity android:name=".ExampapertwoActivity"></activity>
    </application>

</manifest>